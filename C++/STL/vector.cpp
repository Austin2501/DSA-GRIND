 #include <bits/stdc++.h>
using namespace std;
int main(){

        // vector<int> v;
        // v.push_back(1);
        // v.emplace_back(2);
        // v.emplace_back(7);
        // v.emplace_back(6);
        // v.emplace_back(9);

        // vector<pair<int,int>>vec;

        // vec.push_back({1,2});
        // vec.emplace_back(2,3);

        // vector<int> d(5); //size=5 but we can insert beyond 5

        // vector<int> g(5,100); // 5 places initailize with 100 

        // vector<int> g1(5,30);
        // vector<int> g2(g1); //a copy of g2

        // for(vector<int> :: iterator i=v.begin();
        // i!=v.end(); i++){
        //     cout<< *(i)<<" ";
        // }
        //if I write i instead of *(i) it will give address of element
        //v.begin and v.end pointing in memory where the 1st and last element is.
        // cout<<g1[0]<<g1[2];

        // vector<<int>::iterator it=v.end; //it point just right after value of last index
        // vector<<int>::iterator it=v.rend; //reverse end. it is reverse iterator point to 1st value
        // vector<<int>::iterator it=v.rbegin; // reverse begin. pointing to lst value...if I do it++ it will run backward(reverse).

        // cout<<v.back(); //last value

        // for(auto i=v.begin();
        // i!=v.end(); i++){
        //     cout<< *(i)<<" ";

        // }

        //short wat to print all vectors
        // cout<<endl;
        // for(auto i :v ){
        //     cout<< i<<" ";

        // }

        //erase function
     
        // v.erase(v.begin());

        //  cout<<endl;
        // for(auto i :v ){
        //     cout<< i<<" ";

        // }

        //erase multiple
        // v.erase(v.begin()+1,v.begin()+4); //v.erase(start,end) end should be one value greater

        //  cout<<endl;
        // for(auto i :v ){
        //     cout<< i<<" ";

        // }
//////////////insert
        // vector<int> v(2,100); //creates 2 instances of 100 in container
        // v.insert(v.begin(),300) ; //{300,100,100}
        // v.insert(v.begin()+1,2,10); //{300, 10,10,100,100}

        // //copy
        // vector<int> copy(2,50); //50 50
        // v.insert(v.begin(), copy.begin(), copy.end()); //{50,50,300,10,10,100,100}

        //  cout<<endl;
        // for(auto i :v ){
        //     cout<< i<<" ";

        // }

        //cout<<v.size();  give length of vector
        //v.pop_back(); pop last value
        //if v1->{10,20} v2->{30,40}
        //v1.swap(v2);
        // then v1->{30,40} v2->{10,20}
        //v.clear(); //erases the entire vector

        //cout<<v.empty(); //check if vector is empty or not

        




         

    }